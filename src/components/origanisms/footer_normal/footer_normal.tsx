import React from "react";
import {Box} from "@chakra-ui/react";
import {LayoutFooter} from "../layout";

export const FooterNormal:React.VFC=():JSX.Element=>{
    return (
        <Box as={LayoutFooter}>
            day la footer
        </Box>
    )
}